# drone_visualizer
simple AI boundary detection (OpenCV) tuned for orthomosaics  pixel→meter area calculation (supports georeferenced GeoTIFF or manual scale)  a small FastAPI backend to upload/process images and return polygons/areas  helper scripts and a clear README so you can push to GitHub and run locally
